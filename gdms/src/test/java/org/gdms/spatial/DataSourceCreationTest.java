package org.gdms.spatial;

import org.gdms.SourceTest;

public class DataSourceCreationTest extends SourceTest {
	// public void testCreation() throws Exception {
	// File f = new File("src/test/puntos_vacio.shp");
	// if (f.exists())
	// assertTrue(f.delete());
	// f = new File("src/test/puntos_vacio.shx");
	// if (f.exists())
	// assertTrue(f.delete());
	// f = new File("src/test/puntos_vacio.dbf");
	// if (f.exists())
	// assertTrue(f.delete());
	// DefaultSpatialDriverMetadata dsdm = new DefaultSpatialDriverMetadata();
	// dsdm.addSpatialField("geom", SpatialDataSource.POINT);
	// dsdm.addField("campo2", DBFDriver.STRING, new String[]{DBFDriver.LENGTH},
	// new String[]{"5"});
	// ds.createDataSource(new FileSourceCreation(new
	// File("src/test/puntos_vacio.shp"), dsdm));
	//
	// SpatialDataSource d = ds.getSpatialDataSource(new
	// File("src/test/puntos_vacio.shp"));
	// d.start();
	// d.stop();
	// assertTrue(true);
	// }
}
